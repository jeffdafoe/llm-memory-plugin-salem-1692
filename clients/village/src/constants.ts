// Tile dimensions in pixels (render size — wang sheet is 16px, scaled up 2x)
export const TILE_SIZE = 32;

// Wang tile source size in the spritesheet (native 16px)
export const WANG_SRC_SIZE = 16;

// Camera zoom bounds
export const MIN_ZOOM = 0.4;
export const MAX_ZOOM = 2.5;
export const ZOOM_SPEED = 0.1;
export const DEFAULT_ZOOM = 0.72;

// Top bar height in pixels
export const TOP_BAR_HEIGHT = 40;
